import test from'node:test';import assert from'node:assert/strict';import{detectReportIntent,dossierFileName,markdownToDossier,renderDossierHTML,validateDossier}from'../src/dossier-engine.js';
test('estrutura dossie',()=>{const d=markdownToDossier('# Caso\n\n## Resumo executivo\nResumo.\n\n## Cronologia\n- 24/09/2026 - Publicacao.\n\n## Evidencias\n- [oficial] Confirmado.\n\n## Riscos\n- Medio | Risco.\n\n## Plano de acao\n- Alta | Validar.\n\n## Fontes\n- https://example.com',{meta:{kind:'dossie-juridico'}});assert.ok(d.sections.length>=6);assert.equal(d.timeline.length,1);assert.equal(d.evidence[0].kind,'oficial');assert.equal(d.risks[0].level,'medio');assert.equal(d.actions[0].priority,'alta');assert.equal(d.sources[0].url,'https://example.com')});
test('escapa html',()=>{const h=renderDossierHTML(markdownToDossier('# <script>alert(1)</script>\n\n## Resumo executivo\n<img src=x onerror=alert(2)>'));assert.equal(h.includes('<script>alert(1)</script>'),false);assert.equal(h.includes('<img src=x onerror=alert(2)>'),false);assert.ok(h.includes('&lt;script&gt;'))});
test('qualidade e arquivo',()=>{const d=markdownToDossier('# Meu relatorio\n\n## Resumo executivo\nResumo.');const q=validateDossier(d);assert.ok(q.score>=0&&q.score<=100);assert.match(dossierFileName(d),/^meu-relatorio-\d{4}-\d{2}-\d{2}\.html$/)});
test('detecta intencao',()=>{assert.deepEqual(detectReportIntent('Faca um dossie juridico completo'),{wantsReport:true,kind:'dossie-juridico'});assert.equal(detectReportIntent('envie uma mensagem no WhatsApp').wantsReport,false)})