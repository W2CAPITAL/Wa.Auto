name: Verificar WA.Auto Cloud
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Instalar dependencias
        run: npm install --no-fund
      - name: Verificar codigo e testes
        run: npm run verify
      - name: Instalar Chrome apenas para teste visual
        run: npx puppeteer browsers install chrome
      - name: Testar interface
        run: npm run test:browser
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: interface-verificada
          path: test-results/
          if-no-files-found: ignore
          retention-days: 7

  whatsapp-cloud:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Instalar dependencias
        run: npm install --no-fund
      - name: Exigir QR real via Baileys sem Chromium
        run: npm run test:baileys-live
